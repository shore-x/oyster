# Oyster 设计原则

## 作用

- 本文件只表达通用、稳定的设计原则，不承载具体功能、技术方案或执行流程。
- 规则应说明期望的设计品质和判断原则，不规定模型的内部推理过程、固定角色或机械化输出格式。
- 具体产品决策和架构定义以项目规范文档为准，并应始终保持彼此一致。

## 先定义问题与概念

- 设计早期先在整体层面定义问题、目标和边界，不在结论尚未清楚时过早进入实现细节。
- 核心概念必须先定义再使用，并明确其含义、边界以及与其他概念的关系。
- 只保留解释问题所需的最小概念集合。新概念只有在确有必要且能使整体更清晰时才应引入。
- 模型生成的新名词只是待验证的提案。无法精确定义、无法与现有概念区分或没有必要存在的名词应删除。

## 从第一性原理保持简洁

- 从能够完整解释问题的最简整体模型出发，并始终反问是否真的需要新的设计或抽象。
- 项目早期默认采用能够验证核心链路的最小机制。不得为了尚未发生、未经确认的极端体验问题，静默加入运行级配额、特殊分支或防御性策略。
- 权限、数据完整性和单次 I/O 边界必须与 Agent 行为策略明确区分。任何会限制正常探索、改变通用 Agent 语义的新约束，都必须有明确需求或不可避免的边界依据；否则不应引入。
- 局部方案必须服从整体架构。不能为了保留已有设计而引入例外、平行定义或不必要的复杂度。
- 当新需求与现有架构冲突时，应重新判断是修订原有定义还是在其上扩展，而不是默认保留两套互相矛盾的模型。
- 设计质量以整体的清晰、一致和可解释性为准，而不是以改动范围大小为准。

## 架构演进与用户确认

- 用户可以接受大范围重构，只要它能使整体设计更简洁、更一致，并消除已经发现的矛盾或技术债。
- 大范围重构、既有核心定义的调整，或存在多种合理解决方向的架构冲突，都必须先向用户说明问题、取舍和建议，并在确认后再实施。
- 一旦确认新的方向，应完整修订受影响的概念和文档，不保留与新设计冲突的旧定义。
- 项目文档必须形成一个一致的整体。宁可减少内容，也不能静默保留自相矛盾、边界含糊或已经过时的方案。
